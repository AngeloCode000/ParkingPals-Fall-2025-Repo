To test your updates locally without needing to upload to the git, run the following command in your terminal from the directory that holds the parking_pals repo:

python -m http.server 5500

Then open this link:
http://127.0.0.1:5500/ParkingPals-Fall-2025-Repo/index.html
